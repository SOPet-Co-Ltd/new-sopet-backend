/** Bcrypt cost factor — must match AuthService.hashPassword (10). */
export const BCRYPT_ROUNDS = 10;

export const DEV_ADMIN_EMAIL = 'admin@sopet.org';
export const DEV_VENDOR_EMAIL = 'vendor@sopet.org';
export const PROD_ADMIN_EMAIL = 'admin@sopet.org';

export const SEED_PASSWORD = 'P@ssw0rd';
