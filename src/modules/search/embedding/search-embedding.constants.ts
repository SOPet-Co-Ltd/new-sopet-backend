export const SEARCH_EMBEDDING_QUEUE = 'search-embedding';

export const EMBEDDING_MODEL = 'text-embedding-3-small';
export const EMBEDDING_DIMENSION = 1536;

export type SearchEmbeddingJobData = {
  productId: string;
};
